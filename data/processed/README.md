# Processed data

Large processed pass-level files are not committed. Generate them by running the numbered scripts in `scripts/`, beginning with `02_prepare_pass_data.py`. Recommended local outputs are written under `outputs/`, which is ignored by Git.
