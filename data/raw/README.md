# Raw Data

This folder is intended for raw pass-event data downloaded from StatsBomb Open Data.

Large raw CSV files are not committed to this repository. They can be regenerated by running:

```bash
python scripts/01_fetch_statsbomb_passes.py \
  --output_dir data/raw \
  --output_csv passes_all_matches_fixed.csv
