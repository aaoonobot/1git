# Reference outputs

This directory contains compact audit and summary files used to verify the published workflow without committing the large event-level datasets.

- `preprocessing/` contains the preprocessing, target-audit, and match-split summaries.
- `three_team/` contains compact team and player summary tables from the fixed three-team application.

These files are verification artifacts, not substitutes for rerunning the analysis from the fixed manifests.
